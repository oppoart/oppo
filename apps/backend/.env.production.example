# =====================================
# OPPO Production Environment Configuration
# =====================================

# Server Configuration
NODE_ENV=production
PORT=3001
FRONTEND_URL=https://your-domain.com

# Database Configuration (Use managed database service)
DATABASE_URL="postgresql://username:password@your-db-host:5432/oppo_prod?sslmode=require"

# Authentication & Security (Use strong secrets in production)
JWT_SECRET=GENERATE_A_STRONG_SECRET_AT_LEAST_64_CHARACTERS_LONG_FOR_PRODUCTION_USE
BCRYPT_ROUNDS=14
SESSION_TIMEOUT_HOURS=8

# External APIs (Required for Sprint 3 - AI Discovery Features)
OPENAI_API_KEY=prod_openai_api_key_here
FIRECRAWL_API_KEY=prod_firecrawl_api_key_here

# Rate Limiting Configuration (More restrictive in production)
RATE_LIMIT_WINDOW_MS=300000
RATE_LIMIT_MAX_REQUESTS=50
AUTH_RATE_LIMIT_MAX=3

# Logging Configuration (Less verbose in production)
LOG_LEVEL=warn

# CORS Configuration (Restrict to production domain)
CORS_ORIGIN=https://your-domain.com

# Admin Configuration
ADMIN_EMAIL=admin@your-domain.com

# Health Check Configuration
HEALTH_CHECK_ENABLED=true

# =====================================
# Production Security Checklist:
# =====================================
# 
# [ ] Use managed database service (AWS RDS, Google Cloud SQL, etc.)
# [ ] Enable SSL/TLS for database connections
# [ ] Use environment secrets management (AWS Secrets Manager, etc.)
# [ ] Generate strong JWT secrets (use crypto.randomBytes(64).toString('hex'))
# [ ] Enable SSL certificates for HTTPS
# [ ] Configure proper CORS origins
# [ ] Use CDN for static assets
# [ ] Enable monitoring and logging
# [ ] Set up backup and disaster recovery
# [ ] Configure firewall and security groups
# [ ] Use container orchestration (Docker, Kubernetes)
# [ ] Set up CI/CD pipeline with security scanning
# [ ] Enable API rate limiting and DDoS protection
# [ ] Configure health checks and alerts
#